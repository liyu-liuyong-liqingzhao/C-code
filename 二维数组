#include<stdio.h>
int main(){
    int a[3][4]={0};
    printf("%d",sizeof(a[0]));   //每一行相当于二维数组的一个元素，二维数组简化成一维数组。因此输出第一行的大小16.
    printf("%d",sizeof(a[0]+1)); //a[0]表示第一行首元素地址，因此a[0]+1表示第一行第二个元素地址，输出4.
    printf("%d",sizeof(a+1));    //第二行地址。
    printf("%d",sizeof(&a[0]+1); //第二行地址
    printf("%d",sizeof(a[3]);    //sizeof不会真的参与计算，直接根据数组类型来计算大小，因此a[3]虽然超出数组但是本质上等于a[0]，输出16.
    
    int a[3][2]={(0,1),(2,3),(4,5)};   //逗号表达式以最后一个式子为结果，实际上只初始化了1,3,5三个元素。
    
    return 0;
    }

#include<stdio.h> 
int main(){           //整型提升：无符号数左边补0，有符号数左边补符号位。
    char a=128;       //强制转化为char会先取补码最右边的8位再整型提升。
    char b=-128;      //10000000定义为-128,01111111表示127，而128为127+1，可将128也看作10000000。
    printf("%u",a);   //因此最后打印结果相同都为10000000左边补上24个1.
    printf("%u",b);   //%u打印无符号十进制数。
    return 0;
    }

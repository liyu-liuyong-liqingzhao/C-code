#include<stdio.h>
int mian(){
    int a=20;
    //二进制码转化成16进制码再储存
    //00000000000000000000000000010100
    //转化为0x00000014，前面的0x是标识，16进制码在内存中是反向储存的。
    //即在内存中显示14 00 00 00.（每两位一组）
    //大端模式：数据的高位保存在低地址，低位保存在高地址。
    //小端模式与之相反。
    return 0;
    }

#include<stdio.h>
int main(){
    printf("%p",arr);    //除了sizeof和数组名取地址之外，数组名表示首元素地址。
    
    printf("%p",&arr);      //表示整个数组的地址，但是输出仍为首元素地址，因为数组从第一个元素开始，所以输出值一样，但实际上包含整个数组的地址。
    
    return 0;
    }

数据结构：栈
从上往下传参，取参时只能从上往下取，所以先传入的后传出，后传入的先传出。

int function(x,y)    //传参时从右向左，先传b再传a.
function(a,b)      

栈区先使用高地址空间再使用低地址空间。
数组中随着下标增加，地址升高。
int i=0;
int arr[5]={0};
先将i放在高地址，数组arr放在低地址。
但是数组所占的5个空间中，下标为0的地址最低，下标为4的地址最高。
